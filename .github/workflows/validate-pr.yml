name: "👮‍♂️ IEEE Gatekeeper: Verify"

on:
  pull_request:
    paths:
      - "00_Inbox/**.md"  # Triggers when files in Inbox change

jobs:
  validate-submission:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install python-frontmatter

      # --- LEVEL 1: METADATA CHECK ---
      - name: 1️⃣ Validate Metadata (ID Check)
        run: python3 99_System/Scripts/validate_pr.py

      # --- LEVEL 2: SMART LINK CHECK ---
      - name: 2️⃣ Smart Link Audit (Content Check)
        run: python3 99_System/Scripts/smart_linker.py